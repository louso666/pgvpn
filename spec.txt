📄 Схема туннелей и сервисов (сентябрь 2025)
1. Участники инфраструктуры

pg.louso.ru (Россия, основной хаб)
• WireGuard wg200 → 10.200.0.1/24 (точка входа клиентов)
• WireGuard wg-ipsec → 10.10.1.1/24 (канал в Европу / NL)
• WireGuard wg-usa → 10.10.2.1/24 (канал в Америку / USA)
• dnsproxy (10.200.0.1:53) + Telegram-бот
• NAT для 10.200.0.0/24 → eth0 (SNAT/MASQ)
• Policy-routing:
▸ ipset nl_proxy → MARK 0x1 → table 101 → default dev wg-ipsec
▸ ipset usa_proxy → MARK 0x2 → table 102 → default dev wg-usa
• IPTables и ipset конфиги сохраняются через ipset-restore.service и iptables-restore.service
• Генерация клиентских WG-конфигов скриптом /root/wg (бот отдаёт их пользователю)

ipsec.louso.ru (Амстердам, NL-шлюз)
• Интерфейс WireGuard (peer wg-ipsec)
• NAT/MASQUERADE для подсетей от pg.louso.ru
• iptables: разрешение и маскарадинг трафика из туннеля → eth0
• MSS clamp для TCP через WG

usa.louso.ru (Америка, USA-шлюз)
• Интерфейс WireGuard (peer wg-usa)
• NAT/MASQUERADE для подсетей от pg.louso.ru
• iptables: разрешение и маскарадинг трафика из туннеля → eth0
• MSS clamp для TCP через WG

2. Динамические файлы (не синхронизируем)

/root/site_nl и /root/site_usa — списки доменных паттернов, обновляемые ботом

/root/map.json — история резолвов (dnsproxy)

/etc/wireguard/* — серверный конфиг + клиентские конфиги от скрипта /root/wg

/etc/ipset.conf — снимок ipset, меняется рантаймом

3. Статические файлы (синхронизируем)

/etc/iptables/** — rules.v4/v6

/etc/systemd/system/*.service — ipset-restore, policy-routing, wg-quick юниты

Скрипты управления (pull-configs.sh, push-configs.sh и т. д.)

4. Watchdog’и / автоподъём

wg-quick@wg200.service — точка входа клиентов

wg-quick@wg-ipsec.service и wg-quick@wg-usa.service — выходные туннели

ipset-restore.service — поднимает ipset на старте

iptables-restore.service или netfilter-persistent — поднимает правила

policy-routing.service — добавляет ip rule и маршруты для NL/USA

dnsproxy + бот → в рантайме раскладывают IP по ipset и ведут логи

5. Деплой

pull-configs.sh — rsync конфиги с серверов (исключая динамику)

правки и контроль в git

push-configs.sh — rsync обратно на серверы + iptables-restore и рестарт юнитов